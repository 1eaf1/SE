# Тема 6. Базовые коллекции: словари, кортежи
Отчет по Теме #6 выполнил(а):
- Фалалеев Евгений Алексеевич
- АИС-22-1

| Задание | Лаб_раб | Сам_раб |
| ------ | ------ | ------ |
| Задание 1 | + | + |
| Задание 2 | + | + |
| Задание 3 | + | + |
| Задание 4 | + | - |
| Задание 5 | + | - |

знак "+" - задание выполнено; знак "-" - задание не выполнено;

Работу проверили:
- к.э.н., доцент Панов М.А.

## Лабораторная работа №1
### В школе, где вы учились, узнали, что вы крутой программист и попросили написать программу для учителей, которая будет при вводе кабинета писать для него ключ доступа и статус, занят кабинет или нет. При написании программы необходимо использовать словарь (dict), который на вход получает номер кабинета, а выводит необходимую информацию. Если кабинета, который вы ввели нет в словаре, то в консоль в виде значения ключа нужно вывести “None” и виде статуса вывести “False”.

```python
request = int(input("Введите номер кабинета "))

dictionary = {
    101: {'key':1574,'access':True},
    102: {'key':1885,'access':True},
    103: {'key':6747,'access':True},
    104: {'key':5555, 'access': False},
    None: {'key': None, 'access': False}
}

response = dictionary.get(request)
if not response:
    response=dictionary[None]
key = response.get('key')
access = response.get('access')
print(key,access)
```
### Результат.
![Лаб1](https://github.com/1eaf1/SE/blob/Тема_6/pic/Лаб1.png)

## Выводы
В данном коде на ввод подаётся номер кабинета в виде целого числа. В словаре dictionary ключ представляет собой номер кабинета, а значение - ещё один словарь, стостоящий из ключей key и access и их значений. Далее создаётся переменная response, которая получает значение номера кабинета, т.е словарь с key и access. Если указанного пользователем номера кабинета не существует, то присваивается значение None из словаря. Далее переменной key присваивается значение ключа key, а переменной access значение ключа access. Затем две переменные выводятся на экран консоли. 

## Лабораторная работа №2
### Алексей решил создать самый большой словарь в мире. Для этого он придумал функцию dict_maker (**kwargs), которая принимает неограниченное количество параметров «ключ: значение» и обновляет созданный им словарь my_dict, состоящий всего из одного элемента «first» со значением «so easy». Помогите Алексею создать данную функцию.

```python
from pprint import pprint

my_dict = {'first':'so easy'}

def dict_maker(**kwargs):
    my_dict.update(kwargs)

dict_maker(a1=1,a2=2,a3=3,second='fifty fifty',color_eyes='blue',name='John',weight=65)
pprint(my_dict)
```
### Результат
![Лаб2](https://github.com/1eaf1/SE/blob/Тема_6/pic/Лаб2.png)

## Выводы
В данном коде создаётся словарь my_dict с единственным ключом и значением. Далее создаётся функция, которая принимает неограниченное количество элементов и добавляет их в словарь, обновляя словарь my_dict.

## Лабораторная работа №3
### Для решения некоторых задач бывает необходимо разложить строку на отдельные символы. Мы знаем что это можно сделать при помощи split(), у которого более гибкая настройка для разделения для этого, но если нам нужно посимвольно разделить строку без всяких условий, то для этого мы можем использовать кортежи (tuple). Для этого напишем любую строку, которую будем делить и “обвернем” ее в tuple и дальше мы можем как нам угодно с ней работать, например, сделать ее списком (тогда получится полный аналог split()) или же работать с ним дальше, как с кортежем.

```python
input_string = 'HelloWorld!'
result = tuple(input_string)
print(result)
print(list(result))
```

### Результат
![Лаб3](https://github.com/1eaf1/SE/blob/Тема_6/pic/Лаб3.png)

## Выводы
В результате выполнения данного кода каждый символ строки разбился на отдельные элементы, где каждый символ строки представляет собой отдельный элемент кортежа. В первом print() выводится кортеж, а во втором print(list(result)) результат выводится в виде списка.
  
## Лабораторная работа №4
### Вовочка решил написать крутую функцию, которая будет писать имя, возраст и место работы, но при этом на вход этой функции будет поступать кортеж. Помогите Вовочке написать эту программу.

```python
def NameAgePlace(name,age,place="unnamed"):
    print(f"Имя: {name}, Возраст: {age}, Место работы: {place}")

first = ("Григорий",22)
second = ("Василий", 35,"Yandex")
third = ("Вячеслав",27,"Контур")

NameAgePlace(*first)
NameAgePlace(*second)
NameAgePlace(*third)
```

### Результат
![Лаб4](https://github.com/1eaf1/SE/blob/Тема_6/pic/Лаб4.png)

## Выводы
В результате выполнения данного кода в консоли выводятся переданные в функцию аргументы кортежа. Создаётся три кортежа, элементы которых затем передаются в функцию.

## Лабораторная работа №5
### Для сопровождения первых лиц государства X нужен кортеж, но никто не может определиться с порядком машин, поэтому вам нужно написать функцию, которая будет сортировать кортеж, состоящий из целых чисел по возрастанию, и возвращает его. Если хотя бы один элемент не является целым числом, то функция возвращает исходный кортеж.

```python
def tuple_sort(tupl):
    for element in tupl:
        if not isinstance(element,int):
            return tupl
    return tuple(sorted(tupl))

if __name__=="__main__":
    print(tuple_sort((1,4,7,9,2,5,9,7,10)))
    print(tuple_sort((1,0,2,34,'1',2.1)))
```

### Результат
![Лаб5](https://github.com/1eaf1/SE/blob/Тема_6/pic/Лаб5.png)

## Выводы
В данном коде создаётся функция tuple_sort, которая проверяет каждый элемент на несоответствие типу int. Если все элементы соответствуют int, то функцией возвращается отсортированный по возрастанию кортеж, если не все элементы соответствуют int, то обратно возвращается неотсортированный кортеж. В точке входа осуществляется вызов функции.

## Самостоятельная работа №1
### При создании сайта у вас возникла потребность обрабатывать данные пользователя в странной форме, а потом переводить их в нужные вам форматы. Вы хотите принимать от пользователя последовательность чисел, разделенных пробелом, а после переформатировать эти данные в список и кортеж. Реализуйте вашу задумку. Для получения начальных данных используйте input(). Результатом программы будет выведенный список и кортеж из начальных данных.

```python
def transformToListAndTuple(user_input):
    my_list = []
    my_list.extend(user_input)
    my_tuple = tuple(my_list)
    print(my_list)
    print(my_tuple)

if __name__ == "__main__":
    user_input = input().split(" ")
    transformToListAndTuple(user_input)
```
### Результат
![Сам1](https://github.com/1eaf1/SE/blob/Тема_6/pic/Сам1.png)

##  Выводы
В данном коде создаётся функция, которая принимает на вход пользовательский ввод, содержащий числа, которые разделены при помощи пробела. Далее в функции создаётся список, который расширяется от данных пользовательского ввода. Затем создаётся кортеж на основе списка, и дальше созданные список и кортеж выводятся на экран.
  
## Самостоятельная работа №2
### Николай знает, что кортежи являются неизменяемыми, но он очень упрямый и всегда хочет доказать, что он прав. Студент решил создать функцию, которая будет удалять первое появление определенного элемента из кортежа по значению и возвращать кортеж без него. Попробуйте повторить шедевр не признающего авторитеты начинающего программиста. Но учтите, что Николай не всегда уверен в наличии элемента в кортеже (в этом случае кортеж вернется функцией в исходном виде).

```python
def DeleteElementFromTuple(tupl,element):
    my_list = list(tupl)
    if element in my_list:
        my_list.remove(element)
        return tuple(my_list)
    return tupl

if __name__=="__main__":
    my_tuple1 = (1,2,3)
    my_tuple2 = (1,2,3,1,2,3,4,5,2,3,4,2,4,2)
    my_tuple3 = (2,4,6,6,4,2)
    print(DeleteElementFromTuple(my_tuple1,1))
    print(DeleteElementFromTuple(my_tuple2,3))
    print(DeleteElementFromTuple(my_tuple3,9))
```

### Результат
![Сам2](https://github.com/1eaf1/SE/blob/Тема_6/pic/Сам2.png)

## Выводы
В данном коде создаётся функция, внутри которой создаётся список, которому присваиваются значения переданного кортежа. Далее осуществляется проверка того, есть ли элемент, переданный на удаление, в только что созданном списке. Если да, то первое вхождение элемента удаляется из списка с помощью метода remove(). Если нет, то просто возвращается уже существующий кортеж. В точке входа создаются кортежи и передаются в функцию на выполнение.

## Самостоятельная работа №3
### Ребята поспорили кто из них одним нажатием на numpad наберет больше повторяющихся цифр, но не понимают, как узнать победителя. Вам им нужно в этом помочь. Дана строка в виде случайной последовательности чисел от 0 до 9 (длина строки минимум 15 символов). Требуется создать словарь, который в качестве ключей будет принимать данные числа (т. е. ключи будут типом int), а в качестве значений – количество этих чисел в имеющейся последовательности. Для построения словаря создайте функцию, принимающую строку из цифр. Функция должна возвратить словарь из 3-х самых часто встречаемых чисел, также эти значения нужно вывести в порядке возрастания ключа.

```python
from pprint import pprint


def Dictionary(user_input):
    dictionary = {
        0:user_input.count('0'),
        1:user_input.count('1'),
        2:user_input.count('2'),
        3:user_input.count('3'),
        4:user_input.count('4'),
        5:user_input.count('5'),
        6:user_input.count('6'),
        7:user_input.count('7'),
        8:user_input.count('8'),
        9:user_input.count('9')
    }
    list_dictionary = list(dictionary.items())
    list_dictionary.sort(key = lambda x:x[1])
    most_numbers = list_dictionary[7:10]
    most_numbers.sort(key = lambda x:x[1])
    dictionaryMost = dict(most_numbers)


    return dictionaryMost

if __name__=="__main__":
    user_input = str(input())
    if len(user_input)>=15:
        pprint(Dictionary(user_input))
    else:
        print("Меньше 15 символов...")


```

### Результат
![Сам3](https://github.com/1eaf1/SE/blob/Тема_6/pic/Сам3.png)

## Выводы
В данном коде создаётся функция, которая принимает в себе вводимую с клавиатуры пользователем строку. Внутри функции присутствует словарь, который в качестве значений ключа принимает саму цифру, а в качестве значения - количество повторений данной цифры в строке. Далее весь словарь переводится в список, который сортируется по второму элементу в порядке возрастания и осуществляется срез треёх самых часто встречаемых цифр в строке. Этот срез присваивается новой переменной. Далее этот срез уже сортируется по возрастанию ключа и затем переводится в обратно из списка в словарь. Итогом работы функции является возвращение ею словаря с тремя наиболее часто встречаемыми цифрами в строке. В точке входа осуществляется ввод строки и вызов созданной функции.

  
## Самостоятельная работа №4
- Текст задания
- Оформленный код
- Скрины консоли
- Развернутый вывод
  
## Самостоятельная работа №5
- Текст задания
- Оформленный код
- Скрины консоли
- Развернутый вывод

## Общие выводы по теме
- Развернутый вывод
